import { CoffeeDirectiveDirective } from './coffee-directive.directive';

describe('CoffeeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CoffeeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
